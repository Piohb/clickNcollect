{% extends 'base_admin.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}

    {% embed 'admin/title.html.twig' %}
        {% block sectiontitle %}
            <h2 class="mt-4">Category Item</h2>
            <a href="{{ path('category_index') }}" class="btn btn-secondary">back to list</a>
        {% endblock %}
    {% endembed %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex align-items-center">
        <a href="{{ path('category_edit', {'id': category.id}) }}" class="btn btn-success mr-2">edit</a>

        {{ include('admin/category/_delete_form.html.twig') }}
    </div>

{% endblock %}
